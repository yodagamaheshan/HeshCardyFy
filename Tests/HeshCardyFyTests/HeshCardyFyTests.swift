import XCTest
@testable import HeshCardyFy

final class HeshCardyFyTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HeshCardyFy().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
